defmodule Juvet.Template do
  @moduledoc """
  Represents a single message that is compiled into Elixir code and is sent to different platform endpoints.
  """

  def send_message(view, template, assigns) do
  end
end
